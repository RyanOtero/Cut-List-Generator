<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="ToolTip" BasedOn="{StaticResource {x:Type ToolTip}}">
        <Setter Property="Background" Value="{StaticResource Dark}" />
        <Setter Property="Foreground" Value="{StaticResource Light}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Light}"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Padding" Value="4 0 4 0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Name="Border" Background="{TemplateBinding Background}" CornerRadius="4" BorderThickness="2" BorderBrush="{TemplateBinding Foreground}">
                        <ContentPresenter Margin="3 1 4 3" Height="{TemplateBinding Height}" VerticalAlignment="Top" Width="{TemplateBinding Width}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>